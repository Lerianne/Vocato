#!/bin/zsh
# Launcher: ./coach [--checkin]
cd "$(dirname "$0")"
# Make sure Ollama is running
if ! curl -s --max-time 2 http://localhost:11434/api/version >/dev/null; then
  open -a Ollama --hide
  sleep 5
fi
exec .venv/bin/python coach.py "$@"
