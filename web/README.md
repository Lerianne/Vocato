# Vocato — Website

Marketing site for Vocato. Deploys to Vercel with **Root Directory = `web`**.

_Scaffold pending._ Recommended stack: Next.js (App Router) + Tailwind, using the
Vocato brand from `docs/BRAND-GUIDELINES.md` (deep plum `#3A2A3F`, sage `#7E9A82`,
terracotta `#C8775A`; tagline **"Answer the calling."**).

To scaffold:

```bash
cd web
npx create-next-app@latest .
```
